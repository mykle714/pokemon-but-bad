<ul>
	{{#each pokemon}}
	<li>{{this.name}} - {{this.height}} decimeters - {{this.moves}} - {{this.sprite}} - {{this.stats}} - {{this.types}}</li>
	<img src={{this.sprite}} alt="">
	{{/each}}
</ul>